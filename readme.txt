- starting server = "npm run dev"
- .env file required => MONGO_URI = <put your mongo uri with valid password>
                        JWT_SECRET = <put any string and keep it secret, ex. "mysecretpassword">
                        JWT_EXPIRES = <put any value of time, ex. "24h">
put .env file at root level of the folder (same level than app.js)